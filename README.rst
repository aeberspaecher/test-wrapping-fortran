Wrapping Fortran using Cython
-----------------------------

A short experiment closely following http://www.fortran90.org.

Build with::

  ./waf configure
  ./waf build

Then, wither test ``testwrap.py`` in the build directory or install with

::

  ./waf install
